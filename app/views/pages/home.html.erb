<div class="first-section-home">
  <div class="flex center around h-100 w-100">
    <div class="<%= mobile_device? ? "w-90" : "w-80" %>">
      <div class="spacer-100 page-desktop"></div>
      <h1 class="text-center">ENCONTRE AS MELHORES<br><span class="text-success">EXPERIÊNCIAS DE CICLISMO</span>.</h1>
      <p class="text-white text-center">Competições, eventos, agendamentos e assinaturas de pistas e bike parks: todas experiências de ciclismo você encontra aqui!</p>
      <div class="input-group my-4 text-center">
        <input type="text" class="form-control" placeholder="<%= mobile_device? ? "Busque" : "Eventos, competições, pistas, bike parks, etc..." %>" aria-label="Eventos, competições, pistas, bike parks, etc..." aria-describedby="button-addon2">
        <button class="btn btn-success px-2 px-md-3 text-white text-center" type="button" id="button-addon2">ENCONTRAR EXPERIÊNCIAS</button>
      </div>
      <div class="text-center">
        <button class="btn btn-dark border border-success text-success">SEJA UM PARCEIRO</button>
      </div>
    </div>
  </div>
</div>

<div class="second-section-home container">
  <div class="d-flex justify-content-between mt-5 container mb-3 mobile-arrow">
    <div class="align-self-center">
      <h3>O QUE VOCÊ <span class="text-success">PROCURA?</span></h3>
    </div>
    <div class="invert-image">
      <%= image_tag 'arrows.png', class:"mobile-arrow"  %>
    </div>
  </div>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <%= link_to partners_path do %>
      <div class="col h-100">
        <div class="card bg-dark h-100 d-flex justify-content-center">
          <div class="row">
            <div class="col-5 align-self-center">
                <%= image_tag 'race.png', class:"img-fluid", style: "height: 220, crop: :fill" %>
            </div>
            <div class="col-7 card-body text-center d-flex align-items-center">
              <div>
                <h4 class="text-success">PISTAS E BIKE PARKS</h4>
                <p class="text-white">Encontre pistas e bike parks e garanta seu acesso, agendamento e/ou mensalidade.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
    <%= link_to partners_path do %>
      <div class="col h-100">
        <div class="card bg-dark h-100 d-flex justify-content-center">
          <div class="row">
            <div class="col-5 align-self-center">
              <%= image_tag 'calendar.png', class:"img-fluid", style: "height: 220, crop: :fill" %>
            </div>
            <div class="col-7 card-body text-center d-flex align-items-center">
              <div>
                <h4 class="text-success">Cicloviagens</h4>
                <p class="text-white">Descubra percursos e destinos incríveis para explorar de bike.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
    <%= link_to events_path do %>
      <div class="col h-100">
        <div class="card bg-dark h-100 d-flex justify-content-center">
          <div class="row">
            <div class="col-5 align-self-center">
              <%= image_tag 'flag.png', class:"img-fluid", style: "height: 220, crop: :fill" %>
            </div>
            <div class="col-7 card-body text-center d-flex align-items-center">
              <div>
                <h4 class="text-success">COMPETIÇÕES E EVENTOS</h4>
                <p class="text-white">Se inscreva em competições, pedaladas, desafios e outros eventos de ciclismo.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  </div>
</div>

<div class="fourth-section-home">
  <div class="d-flex justify-content-between mt-5 container mb-3">
    <div>
      <%= image_tag 'arrows.png', class:"mobile-arrow"  %>
    </div>
    <div class="align-self-center">
      <h3>PRÓXIMOS <span class="text-success">EVENTOS</span></h3>
    </div>
  </div>

  <div class="container mt-5 d-none d-sm-none d-md-none d-lg-none d-xl-block">
    <div class="row">
      <% @events.each do |event| %>
        <div class="col-sm-12 col-md-6 col-lg-3 p-3">
          <%= link_to event_path(event), class: "" do %>
            <% if event.photo.attached? %>
                <%= image_tag cl_image_path event.photo.key, height: 220, crop: :fill %>
              <% else %>
                <%= image_tag 'default.png', style: "height: 220, crop: :fill" %>
            <% end %>
          <% end %>
          <div class="bg-dark">
            <div class="d-flex justify-content-between p-1">
              <h4 class="text-success flex-fill"><%= event.name.truncate(13) %></h4>
              <div>
              <h4 class="text-success h5"><%= event.scheduled_start.strftime('%A, %d de %B') %> / <%=  event.scheduled_start.strftime("%H:%M") %></h4>
              </div>
            </div>
            <div class="p-2">
              <p><%= event.description.to_plain_text.truncate(80) %></p>
              <p>Localização - <%= event.city.name %>, <%= event.state.acronym %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <% if @events.present? %>
      <div class="text-center mt-0 mt-md-5 mb-4 mb-mb-0">
        <%= link_to "VER TODOS OS EVENTOS", events_path, class: "btn btn-success" %>
      </div>
    <% else %>
      <p class="m-0 text-white mb-5">Nenhum evento programado até o momento.</p>
    <% end %>
  </div>

  <div class="carousel-mobile page-mobile">
    <div id="carouselExampleCaptions" class="carousel slide .d-none .d-sm-block .d-md-none" data-bs-ride="carousel"  data-bs-interval="false">
      <div class="carousel-inner">
        <% @events.each do |event| %>
          <% if @events.first.id == event.id %>
            <div class="carousel-item active">
          <% else %>
            <div class="carousel-item">
          <% end %>
          <%= link_to event_path(event), class: "" do %>
            <% if event.photo.attached? %>
                <%= image_tag cl_image_path event.photo.key, height: 290, crop: :fill %>
              <% else %>
                <%= image_tag 'default.png' %>
            <% end %>
          <% end %>
            <div class="bg-dark">
              <div class="p-2">
                <h3><span class="text-success"><%= event.name.truncate(13) %></span></h3>
                <h4><span class="text-success"><%= event.scheduled_start.strftime('%A, %d de %B') %> / <%=  event.scheduled_start.strftime("%H:%M") %></span></h4>
              </div>
              <div class="p-2">
                <p><%= event.description.to_plain_text.truncate(80) %></p>
                <p>Localização - <%= event.city.name %>, <%= event.state.acronym %></p>
              </div>
            </div>
          </div>
        <% end %>

        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
    </div>
  </div>

<div class="fifth-section-home mt-5 mt-md-0">
  <div class="container">
    <h2>GERENCIE <span class="text-success">EVENTOS / BIKEPARKS</span></h2>
    <p class="mb-0">Faça a gestão completa do seus eventos e espaços:</p>
    <ul>
      <li>Crie competições, cicloviagens, pedaladas, treinos, desafios ou qualquer outro tipo de evento. </li>
      <li>Cadastre seu espaço e ofereça acessos, agendamentos e planos de mensalidade. </li>
    </ul>
    <button class="btn btn-success me-2">SEJA UM PARCEIRO</button>
    <button class="btn btn-dark border border-success text-success">ENTENDA COMO FUNCIONA</button>
  </div>
  <div class="spacer-100 page-desktop"></div>
  <div class="spacer-50 page-mobile"></div>
</div>
